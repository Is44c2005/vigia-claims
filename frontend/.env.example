# URL del backend desplegado en Render.
# En desarrollo local dejar vacío (el proxy de Vite apunta a localhost:8001).
# En producción (Vercel / Netlify) pegar la URL de Render, sin barra al final:
#   VITE_API_URL=https://vigia-backend.onrender.com/api
VITE_API_URL=
